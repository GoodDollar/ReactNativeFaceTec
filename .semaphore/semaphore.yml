version: v1.0
name: Release

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

execution_time_limit:
  minutes: 5

promotions:
  - name: Release Promotion
    pipeline_file: release_artifact.yml
    auto_promote:
      when: "branch = 'master'"
